# FLOTTE — Exemple de variables d'environnement
# Copier ce fichier en .env et remplir les valeurs (ne jamais commiter .env).

# Connexion Google (bouton « Se connecter avec Google »)
GOOGLE_OAUTH_CLIENT_ID=votre-id-client.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=votre-secret-client

# Optionnel : Django
# DJANGO_SECRET_KEY=...
# DJANGO_DEBUG=1
# ALLOWED_HOSTS=localhost,127.0.0.1

# Liens dans les emails (bienvenue, mot de passe oublié) — défaut en dev : 127.0.0.1:8000
# EMAIL_DOMAIN=127.0.0.1:8000

# SMTP (pour envoi réel) : définir EMAIL_HOST pour activer
# EMAIL_HOST=smtp.gmail.com
# EMAIL_HOST_USER=...
# EMAIL_HOST_PASSWORD=...
